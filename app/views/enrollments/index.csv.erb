<% headers = ['Index', 'Subject ID', 'Home ID', 'RA ID', 'Status', 'Eligibility', 'Secondary', 'Project Name', 'Start Date'] %>
<%= CSV.generate_line(headers).html_safe.strip %>
<% @enrollments.each do |enrollment| %>
  <%= CSV.generate_line([enrollment.id, enrollment.subjId, enrollment.homeId, enrollment.RAId, enrollment.enrollment_state.Name, enrollment.eligibility_state.Title, enrollment.eligibility_sub_state.Title, enrollment.project.name, enrollment.startDate]).html_safe.strip %>
<% end %>
